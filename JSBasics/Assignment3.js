/**
 * Hoisting Example 
Predict the output and explain: 
 */
console.log(a); 
var a = 5; 

/**
 * Output is a = undefined
 * the value of a remain undefined until its initialized. 
 * Here the output is undefined as the value is printed before the initialization takes place.
 *  
 */