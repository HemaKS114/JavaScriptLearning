/**
 * Operator Precedence(Apply BODMAS rule)
 * Predict the output: 
 */
let result = 10 + 5 * 2 / 2 - 1; 
console.log(result);

/**
 * the output: 14
 * 
 * As per BODMAS rule, Brackets, then orders as these are not avialble here.
 * first Divide the number, then multiply, then add and subtract.
 * 
 */